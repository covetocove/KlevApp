Things to do to setup:
	- download libsvm-3.2, and add the python folder in the libsvm-3.2 to your PYTHONPATH
	- download django-background-tasks via "pip install django-background-task"

Starting app:
	- start app through "python manage.py runserver" as normal
	- start running background tasks via "python manage.py process_tasks"
